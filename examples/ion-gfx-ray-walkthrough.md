# Ion Gfx Ray Scope Walkthrough

This walk-through keeps the domain vocabulary close to the data instead of burying it in prose.

| Case | Focus | Score | Lane |
| --- | --- | ---: | --- |
| baseline | geometry span | 188 | ship |
| stress | atlas pressure | 145 | ship |
| edge | shader drift | 242 | ship |
| recovery | render budget | 196 | ship |
| stale | geometry span | 155 | ship |

Start with `edge` and `stress`. They create the widest contrast in this repository's fixture set, which makes them better review anchors than the middle cases.

The useful comparison is `shader drift` against `atlas pressure`, not the raw score alone.
